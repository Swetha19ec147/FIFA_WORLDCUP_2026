{
  "name": "visionary-fifa-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build:api": "npm install && npx prisma generate --schema=apps/api/prisma/schema.prisma && npx prisma db push --schema=apps/api/prisma/schema.prisma && npm run build -w api",
    "build:web": "npm install && npm run build -w @visionary-fifa/web"
  },
  "prisma": {
    "schema": "apps/api/prisma/schema.prisma"
  }
}
